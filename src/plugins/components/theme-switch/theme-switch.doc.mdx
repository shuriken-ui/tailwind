import { Meta, Primary, Controls, Story } from '@storybook/blocks'
import * as ThemeSwitchStories from './theme-switch.stories'
import { defaultConfig } from './theme-switch.config'

<Meta of={ThemeSwitchStories} />

# Theme Switch

The theme switch is a control that lets you toggle between light and dark mode.

<Primary />

## Props

<Controls />

## Variants

<br />

### Inverted

Theme switches can be inverted to be more visible on dark backgrounds.

<div className="border border-muted-200 dark:border-muted-800 bg-white dark:bg-slate-900 p-6 rounded-sm">
  <div className="grid grid-cols-1 md:grid-cols-2 gap-6 w-full max-w-xs">
    <div>
      <Story of={ThemeSwitchStories.InvertedFalse} />
    </div>
    <div>
      <Story of={ThemeSwitchStories.InvertedTrue} />
    </div>
  </div>
</div>

<br />
<br />

## Customization

### Default config

<div className="relative">
  <details className="relative bg-slate-50 border border-slate-200 rounded-lg p-4 [&>summary>svg]:open:-rotate-180">
    <summary className="list-none cursor-pointer">
      <span className="font-sans text-slate-500">View configuration options</span>
      <svg
        className="w-5 h-5 text-slate-500 absolute top-5 end-4 transition-transform duration-300"
        xmlns="http://www.w3.org/2000/svg"
        width="32"
        height="32"
        viewBox="0 0 24 24"
      >
        <path
          fill="none"
          stroke="currentColor"
          strokeLinecap="round"
          strokeLinejoin="round"
          strokeWidth="2"
          d="m6 9l6 6l6-6"
        />
      </svg>
    </summary>

    <div className="!mt-4">
      <pre >
        {JSON.stringify(defaultConfig, null, 2)}
      </pre>
    </div>

  </details>
</div>
